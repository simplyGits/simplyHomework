@require "../vars"

.recentGradeGroup
	position: relative
	z-index: 0

	padding = 5px
	padding-top: padding
	padding-bottom: padding
	margin-bottom: 5px

	fullNoticeWidth()

	font-size: 15px
	cursor: pointer

	.back
		absolute: top left right bottom
		z-index: -1
		opacity: .2

	.prev
		opacity: .7
